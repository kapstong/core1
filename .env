# PC Parts Merchandising System - Environment Configuration
# Copy this file to .env and update with your actual values
# DO NOT commit .env to version control

# Database Configuration
DB_TYPE=mysql
DB_HOST=localhost
DB_NAME=core1_core1merch
DB_USER=core1_karldc
DB_PASSWORD=karlkevin1122!!
DB_CHARSET=utf8mb4

# Application Configuration
APP_ENV=development
APP_DEBUG=true
APP_URL=https://core1.merchandising-c23.com

# Email/SMTP Configuration (optional - can be set in admin panel)
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=noreply@pcparts.com
SMTP_FROM_NAME=PC Parts Central

# Session Configuration
SESSION_LIFETIME=1800
SESSION_SECURE=true

# Security
# Generate a random key for production: openssl rand -base64 32
APP_KEY=

# CORS Configuration (comma-separated origins)
CORS_ALLOWED_ORIGINS=http://localhost,http://127.0.0.1,http://localhost/core1,*
